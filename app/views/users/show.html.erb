<h1>List of events</h1>
<p>All the events you ever created or attended.</p>
<h2>Created events</h2>
<p>All the events you ever created.</p>
<% @user.created_events.each do |event| %>
  <p><%= link_to event.date, event_path(event) %></p>
<% end %>
<div>
  <h2>Past events</h2>
  <p>All the events you attended in the past.</p>
  <% @user.attended_events.past.each do |past_event| %>
    <p><%= link_to past_event.date, event_path(past_event) %></p>
  <% end %>
</div>
<div>
  <h2>Upcoming events</h2>
  <p>All the events you will attend in the future.</p>
  <% @user.attended_events.future.each do |future_event| %>
    <p><%= link_to future_event.date, event_path(future_event) %></p>
  <% end %>
</div>
